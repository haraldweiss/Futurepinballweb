.fpw-config.json
src/public/fpw-config.json
.fpw-server.pid
node_modules/
dist/
release/

# Per-user Claude Code settings (allowlist, etc.) — keep out of the
# shared repo; checked-in policy lives in .claude/settings.json.
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Claude Code worktrees (embedded git repos)
.claude/worktrees/

# OS / editor cruft
.DS_Store
*.log

console-output.txt
scripts/capture-console.cjs
scripts/devlog-convert.mjs
